<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prayer Times — <%= location %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <div class="header-top">
                <h1><span>Prayer Times</span>Salah</h1>
                <div class="location-selector">
                    <form id="locationForm" method="get" class="location-form">
                        <select id="country" name="country" class="location-dropdown" required>
                            <option value="">Country</option>
                            <option value="Belgium">Belgium</option>
                            <option value="United Kingdom">United Kingdom</option>
                            <option value="Germany">Germany</option>
                            <option value="France">France</option>
                            <option value="Netherlands">Netherlands</option>
                            <option value="Sweden">Sweden</option>
                            <option value="Spain">Spain</option>
                            <option value="Italy">Italy</option>
                            <option value="Turkey">Turkey</option>
                            <option value="Egypt">Egypt</option>
                            <option value="Saudi Arabia">Saudi Arabia</option>
                            <option value="United Arab Emirates">United Arab Emirates</option>
                            <option value="Pakistan">Pakistan</option>
                            <option value="Malaysia">Malaysia</option>
                            <option value="Indonesia">Indonesia</option>
                            <option value="Canada">Canada</option>
                            <option value="United States">United States</option>
                            <option value="Australia">Australia</option>
                        </select>
                        <select id="city" name="city" class="location-dropdown" required>
                            <option value="">City</option>
                        </select>
                        <button type="submit" class="location-btn">Go</button>
                    </form>
                </div>
            </div>

            <h2><%= location %></h2>

            <div class="date-strip">
                <div class="date-item">
                    <span class="date-label">Gregorian</span>
                    <span class="date-value"><%= date %></span>
                </div>
                <% if (hijriDate && hijriDate.hijri) { %>
                <div class="date-divider">✦</div>
                <div class="date-item">
                    <span class="date-label">Hijri</span>
                    <span class="date-value hijri"><%= hijriDate.hijri.day + ' ' + hijriDate.hijri.month.en + ' ' + hijriDate.hijri.year + ' AH' %></span>
                </div>
                <% } %>
            </div>
        </header>

        <div class="content-body">

        <% if (error) { %>
            <div class="error-message">
                <p><%= error %></p>
            </div>
        <% } else { %>

            <% if (nextPrayer) { %>
                <div class="next-prayer">
                    <div class="next-prayer-inner">
                        <div class="next-prayer-left">
                            <span class="next-label">Next Prayer</span>
                            <span class="next-name"><%= nextPrayer.name %></span>
                            <span class="next-time"><%= nextPrayer.time %></span>
                        </div>
                        <div class="countdown-wrap">
                            <span class="countdown-label">Time remaining</span>
                            <div class="countdown">
                                <span class="cd-seg"><span class="cd-num" id="cd-h">00</span><span class="cd-unit">h</span></span>
                                <span class="cd-sep">:</span>
                                <span class="cd-seg"><span class="cd-num" id="cd-m">00</span><span class="cd-unit">m</span></span>
                                <span class="cd-sep">:</span>
                                <span class="cd-seg"><span class="cd-num" id="cd-s">00</span><span class="cd-unit">s</span></span>
                            </div>
                        </div>
                    </div>
                </div>
            <% } %>

            <div class="prayer-times-grid">
                <div class="prayer-card">
                    <div class="prayer-card-text">
                        <span class="prayer-name">Fajr</span>
                        <div class="prayer-note">Imsak: <%= prayerTimes.Imsak %></div>
                    </div>
                    <span class="prayer-time"><%= prayerTimes.Fajr %></span>
                </div>
                <div class="prayer-card">
                    <div class="prayer-card-text">
                        <span class="prayer-name">Sunrise</span>
                        <div class="prayer-note">Shuruq</div>
                    </div>
                    <span class="prayer-time"><%= prayerTimes.Sunrise %></span>
                </div>
                <div class="prayer-card">
                    <div class="prayer-card-text">
                        <span class="prayer-name">Dhuhr</span>
                        <div class="prayer-note">Noon Prayer</div>
                    </div>
                    <span class="prayer-time"><%= prayerTimes.Dhuhr %></span>
                </div>
                <div class="prayer-card">
                    <div class="prayer-card-text">
                        <span class="prayer-name">Asr</span>
                        <div class="prayer-note">Afternoon Prayer</div>
                    </div>
                    <span class="prayer-time"><%= prayerTimes.Asr %></span>
                </div>
                <div class="prayer-card">
                    <div class="prayer-card-text">
                        <span class="prayer-name">Maghrib</span>
                        <div class="prayer-note">Sunset: <%= prayerTimes.Sunset %></div>
                    </div>
                    <span class="prayer-time"><%= prayerTimes.Maghrib %></span>
                </div>
                <div class="prayer-card">
                    <div class="prayer-card-text">
                        <span class="prayer-name">Isha</span>
                        <div class="prayer-note">Midnight: <%= prayerTimes.Midnight %></div>
                    </div>
                    <span class="prayer-time"><%= prayerTimes.Isha %></span>
                </div>
            </div>

            <div class="ornament">✦ &nbsp; ✦ &nbsp; ✦</div>

        <% } %>
        </div>

        <footer>
            <p><%= location %></p>
            <p><small>Updated <%= new Date().toLocaleString('en-GB', { dateStyle: 'long', timeStyle: 'short' }) %></small></p>
        </footer>
    </div>

    <script>
        /* ── location selector ── */
        const locationData = {
            'Belgium': ['Antwerp','Brussels','Ghent','Bruges','Liege','Charleroi','Mechelen'],
            'United Kingdom': ['London','Manchester','Birmingham','Leeds','Glasgow','Liverpool','Bristol','Edinburgh'],
            'Germany': ['Berlin','Munich','Hamburg','Cologne','Frankfurt','Dusseldorf','Dresden'],
            'France': ['Paris','Marseille','Lyon','Toulouse','Nice','Strasbourg','Bordeaux'],
            'Netherlands': ['Amsterdam','Rotterdam','Utrecht','The Hague','Eindhoven','Groningen'],
            'Sweden': ['Stockholm','Gothenburg','Malmo','Uppsala','Sundsvall'],
            'Spain': ['Madrid','Barcelona','Valencia','Seville','Bilbao','Cordoba'],
            'Italy': ['Rome','Milan','Naples','Turin','Florence','Venice','Genoa'],
            'Turkey': ['Istanbul','Ankara','Izmir','Bursa','Antalya','Konya'],
            'Egypt': ['Cairo','Alexandria','Giza','Aswan','Luxor'],
            'Saudi Arabia': ['Riyadh','Jeddah','Mecca','Medina','Dammam'],
            'United Arab Emirates': ['Dubai','Abu Dhabi','Sharjah','Ajman','Ras Al Khaimah'],
            'Pakistan': ['Karachi','Lahore','Islamabad','Peshawar','Quetta','Multan'],
            'Malaysia': ['Kuala Lumpur','George Town','Ipoh','Kota Kinabalu','Kuching'],
            'Indonesia': ['Jakarta','Surabaya','Medan','Semarang','Makassar','Palembang'],
            'Canada': ['Toronto','Vancouver','Montreal','Calgary','Ottawa','Edmonton'],
            'United States': ['New York','Los Angeles','Chicago','Houston','Phoenix','Philadelphia','San Francisco'],
            'Australia': ['Sydney','Melbourne','Brisbane','Perth','Adelaide','Canberra']
        };

        const countrySelect = document.getElementById('country');
        const citySelect    = document.getElementById('city');
        const locationForm  = document.getElementById('locationForm');

        countrySelect.addEventListener('change', function() {
            citySelect.innerHTML = '<option value="">City</option>';
            if (this.value && locationData[this.value]) {
                locationData[this.value].forEach(c => {
                    const o = document.createElement('option');
                    o.value = c; o.textContent = c;
                    citySelect.appendChild(o);
                });
            }
        });

        citySelect.addEventListener('change', function() {
            if (this.value) locationForm.submit();
        });

        window.addEventListener('load', function() {
            const loc = '<%= location %>';
            if (loc && loc !== 'undefined') {
                const parts = loc.split(', ');
                if (parts.length === 2) {
                    const city = parts[0].trim(), country = parts[1].trim();
                    for (let i = 0; i < countrySelect.options.length; i++) {
                        if (countrySelect.options[i].value === country) { countrySelect.value = country; break; }
                    }
                    countrySelect.dispatchEvent(new Event('change'));
                    setTimeout(() => {
                        for (let i = 0; i < citySelect.options.length; i++) {
                            if (citySelect.options[i].value === city) { citySelect.value = city; break; }
                        }
                    }, 0);
                }
            }
        });

        /* ── countdown ── */
        <% if (nextPrayer) { %>
        (function() {
            const raw = '<%= nextPrayer.time %>';
            const [tH, tM] = raw.split(':').map(Number);

            function tick() {
                const now = new Date();
                const target = new Date(now);
                target.setHours(tH, tM, 0, 0);
                if (target <= now) target.setDate(target.getDate() + 1);

                const diff = Math.max(0, Math.floor((target - now) / 1000));
                const h = Math.floor(diff / 3600);
                const m = Math.floor((diff % 3600) / 60);
                const s = diff % 60;

                document.getElementById('cd-h').textContent = String(h).padStart(2, '0');
                document.getElementById('cd-m').textContent = String(m).padStart(2, '0');
                document.getElementById('cd-s').textContent = String(s).padStart(2, '0');
            }
            tick();
            setInterval(tick, 1000);
        })();
        <% } %>
    </script>
</body>
</html>
